# ParkHub — Production Docker Compose
# Usage: docker compose up -d

services:
  parkhub:
    image: ghcr.io/nash87/parkhub:latest
    container_name: parkhub
    ports:
      - "7878:7878"
    volumes:
      - parkhub-data:/data
    environment:
      PARKHUB_DATA_DIR: /data
      PARKHUB_PORT: "7878"
      PARKHUB_LOG_LEVEL: info
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:7878/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  # Optional: Caddy reverse proxy with automatic HTTPS
  # Uncomment and set your domain in Caddyfile
  #
  # caddy:
  #   image: caddy:2-alpine
  #   container_name: parkhub-caddy
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./Caddyfile:/etc/caddy/Caddyfile:ro
  #     - caddy-data:/data
  #     - caddy-config:/config
  #   depends_on:
  #     parkhub:
  #       condition: service_healthy
  #   restart: unless-stopped

volumes:
  parkhub-data:
  # caddy-data:
  # caddy-config:
