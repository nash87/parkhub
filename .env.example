# ParkHub Configuration
PARKHUB_HOST=0.0.0.0
PARKHUB_PORT=8080
PARKHUB_DATA_DIR=./parkhub-data

# Admin credentials (only used on first start)
PARKHUB_ADMIN_USER=admin
PARKHUB_ADMIN_PASS=changeme

# TLS (optional - auto-generates self-signed if not set)
# PARKHUB_TLS_ENABLED=true
# PARKHUB_TLS_CERT=/path/to/cert.pem
# PARKHUB_TLS_KEY=/path/to/key.pem

# Logging
RUST_LOG=info
